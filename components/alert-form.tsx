"use client"; import { useState } from "react"; import { Bell } from "lucide-react"; import { Button } from "@/components/ui/button";
export function AlertForm(){const [msg,setMsg]=useState("");const [busy,setBusy]=useState(false);async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/alerts",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f.entries()))});const d=await r.json() as {message?:string,error?:string};setMsg(d.message??d.error??"");setBusy(false)}return <form onSubmit={go} className="rounded-2xl bg-[#071a2e] p-6 text-white md:p-8"><Bell className="text-[#dfc27f]"/><h2 className="mt-3 text-2xl font-black">تنبيهات الوظائف</h2><p className="mt-2 text-slate-300">توصل بالفرص الجديدة المناسبة مباشرة.</p><div className="mt-5 flex flex-col gap-3 sm:flex-row"><input name="email" type="email" required className="h-12 flex-1 rounded-xl bg-white px-4 text-[#071a2e] outline-none" placeholder="بريدك الإلكتروني"/><Button disabled={busy} className="h-12 rounded-xl bg-[#00a67e] px-6 font-black">{busy?"جارٍ التسجيل...":"اشترك مجاناً"}</Button></div>{msg&&<p className="mt-3 text-sm font-black text-[#dfc27f]">{msg}</p>}</form>}
