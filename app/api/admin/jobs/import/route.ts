import { getDb } from "@/db"; import { jobs } from "@/db/schema"; import { getChatGPTUser } from "@/app/chatgpt-auth";
type ImportJob={slug?:string;titleAr?:string;titleEn?:string;company?:string;country?:string;city?:string;category?:string;employmentType?:string;workMode?:string;salary?:string;descriptionAr?:string;requirementsAr?:string[];status?:string};
export async function POST(request:Request){if(!await getChatGPTUser())return Response.json({error:"غير مصرح"},{status:401});const input=await request.json();if(!Array.isArray(input)||!input.length)return Response.json({error:"أرسل قائمة وظائف JSON"},{status:400});let added=0,skipped=0;for(const raw of input as ImportJob[]){if(!raw.titleAr||!raw.company||!raw.country||!raw.city||!raw.descriptionAr){skipped++;continue}const slug=(raw.slug??raw.titleAr).toLowerCase().trim().replace(/\s+/g,"-").replace(/[^\p{L}\p{N}-]/gu,"").slice(0,90);try{await getDb().insert(jobs).values({slug,titleAr:raw.titleAr,titleEn:raw.titleEn??"",company:raw.company,country:raw.country,city:raw.city,category:raw.category??"عام",employmentType:raw.employmentType??"دوام كامل",workMode:raw.workMode??"حضوري",salary:raw.salary??"",descriptionAr:raw.descriptionAr,requirementsAr:JSON.stringify(raw.requirementsAr??[]),status:raw.status==="published"?"published":"draft",publishedAt:raw.status==="published"?new Date().toISOString():null});added++;}catch{skipped++;}}return Response.json({added,skipped});}
