"use client";
import{useState}from"react";
export function ContactForm(){const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function send(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg("");const form=e.currentTarget;const body=Object.fromEntries(new FormData(form).entries());const r=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const data=await r.json() as{error?:string};setMsg(r.ok?"تم إرسال رسالتك بنجاح":data.error??"تعذر الإرسال، حاول لاحقاً");if(r.ok)form.reset();setBusy(false)}const c="mt-2 h-12 w-full rounded-xl border px-4 outline-none focus:border-[#00a67e]";return <form onSubmit={send} className="rounded-2xl border bg-white p-6"><input name="website" tabIndex={-1} autoComplete="off" aria-hidden="true" className="hidden"/><div className="grid gap-5 sm:grid-cols-2"><label className="font-black">الاسم<input name="name" required maxLength={100} className={c}/></label><label className="font-black">البريد الإلكتروني<input name="email" type="email" required maxLength={160} className={c}/></label><label className="font-black sm:col-span-2">الموضوع<input name="subject" required maxLength={160} className={c}/></label><label className="font-black sm:col-span-2">الرسالة<textarea name="message" required minLength={10} maxLength={3000} className="mt-2 min-h-36 w-full rounded-xl border p-4 outline-none focus:border-[#00a67e]"/></label></div><button disabled={busy} className="mt-5 rounded-xl bg-[#00a67e] px-6 py-3 font-black text-white disabled:opacity-50">{busy?"جارٍ الإرسال...":"إرسال الرسالة"}</button>{msg&&<p className="mt-4 font-bold text-[#008f6c]" role="status">{msg}</p>}</form>}
