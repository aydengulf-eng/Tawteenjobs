"use client";import {useState} from "react";import {Button} from "@/components/ui/button";
export function ContactForm(){const[msg,setMsg]=useState("");async function send(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f.entries()))});setMsg(r.ok?"تم إرسال رسالتك بنجاح":"تعذر الإرسال، حاول لاحقاً");if(r.ok)e.currentTarget.reset()}const c="mt-2 h-12 w-full rounded-xl border px-4 outline-none focus:border-[#00a67e]";return <form onSubmit={send} className="rounded-2xl border bg-white p-6"><div className="grid gap-5 sm:grid-cols-2"><label className="font-black">الاسم<input name="name" required className={c}/></label><label className="font-black">البريد الإلكتروني<input name="email" type="email" required className={c}/></label><label className="font-black sm:col-span-2">الموضوع<input name="subject" required className={c}/></label><label className="font-black sm:col-span-2">الرسالة<textarea name="message" required minLength={10} className="mt-2 min-h-36 w-full rounded-xl border p-4 outline-none focus:border-[#00a67e]"/></label></div><Button className="mt-5 h-12 rounded-xl bg-[#00a67e] px-7 font-black">إرسال الرسالة</Button>{msg&&<p className="mt-3 font-black text-[#008f6c]">{msg}</p>}</form>}
