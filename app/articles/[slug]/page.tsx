import { notFound } from "next/navigation"; import { SiteHeader } from "@/components/site-header"; import { articles } from "@/lib/content";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);return {title:a?`${a.title} | توطين`:"المقال غير موجود",description:a?.excerpt}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();return <main dir="rtl" className="min-h-screen bg-[#f5f8fa]"><SiteHeader/><article className="container-shell max-w-3xl py-12"><span className="font-black text-[#009b78]">{a.category} • {a.read}</span><h1 className="mt-4 text-3xl font-black leading-[1.5] text-[#071a2e] md:text-4xl">{a.title}</h1><p className="mt-4 text-lg leading-8 text-slate-500">{a.excerpt}</p><div className="mt-8 rounded-2xl border bg-white p-6 text-[1.05rem] leading-9 text-slate-700"><h2 className="text-2xl font-black text-[#071a2e]">ابدأ من متطلبات الوظيفة</h2><p className="mt-4">اقرأ الإعلان بعناية وحدد المهارات والخبرات التي يكررها صاحب العمل. رتّب معلوماتك حسب أهميتها للوظيفة، واستعمل أمثلة ونتائج واضحة بدل العبارات العامة.</p><h2 className="mt-8 text-2xl font-black text-[#071a2e]">اجعل معلوماتك سهلة القراءة</h2><p className="mt-4">استخدم عناوين واضحة، فقرات قصيرة، وبيانات صحيحة. راجع اللغة والتواريخ، ولا تضف معلومات لا تخدم قرار مسؤول التوظيف.</p><h2 className="mt-8 text-2xl font-black text-[#071a2e]">راجع وخصص قبل الإرسال</h2><p className="mt-4">خصص طلبك لكل فرصة، وتأكد أن بيانات التواصل حديثة وأن الملفات تحمل أسماء احترافية. هذه الخطوات البسيطة ترفع جودة طلبك بشكل ملحوظ.</p></div></article></main>}
